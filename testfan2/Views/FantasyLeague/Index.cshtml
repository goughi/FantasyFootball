@model IEnumerable<testfan2.Models.FantasyLeague>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FantasyLeagueName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FantasyLeagueName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FantasyLeagueId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FantasyLeagueId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FantasyLeagueId })
        </td>
    </tr>
}

</table>
